/* === demo-tabs.js === 演示模式标签已移除，保留空函数避免报错 */

var demoCurrentMode = null;

function initDemoTabs() {
    // 标签栏已移除，不再需要初始化
}

function switchDemoMode(mode) {}
function enterDailyMode() {}
function exitDailyMode() {}
function enterSalaryMode() {}
function enterOriginalMode() {}
